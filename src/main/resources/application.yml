server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/pruedaesntt/bootCamp-ConfigServer.git
          search-paths:
          - '*.yaml'
        default-label: main